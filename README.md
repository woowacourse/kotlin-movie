# kotlin-movie

## 4단계 요구사항 목록

- [x] Spring 의존성 추가
- [x] 기존 프로젝트를 console로 분리
- [x] MovieApiController 구현 및 테스트 코드 작성
  - [x] 영화 목록 조회 API 추가 및 테스트 코드 작성
  - [x] 예매 API 구현 및 ScreeningRepository 상영 조회 기능 추가 및 테스트 코드 작성

## 3단계 시작 전 피드백 적용
- [x] Controller 클래스 내 프로퍼티 가시성 수정 (public -> private)
- [x] Ticket 클래스의 calculate 메서드 내 루프를 forEach로 변경
- [x] ScreeningSchedule 불변 객체 반환이 controller의 schedule에 반영되도록 변경

## 3단계 요구사항 목록

- [x] H2 데이터베이스 의존성 추가
- [x] 데이터베이스 스키마 설계
- [x] JdbcConnection 구현 및 테스트 추가 
- [x] ScreeningRoomRepository 구현 및 테스트 코드 추가
- [x] MovieRepository 구현 및 테스트 코드 추가
- [x] ScreeningRepository 구현 및 테스트 코드 추가
- [x] ReservationRepository 구현 및 테스트 코드 작성

### 데이터베이스 스키마 
#### 1. `movies` (영화 정보)
| 컬럼명 | 타입 | 제약 조건 | 설명 |
| :--- | :--- | :--- | :--- |
| **id** | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 영화 고유 식별자 |
| **title** | VARCHAR(255) | NOT NULL | 영화 제목 |
| **running_time** | INT | NOT NULL | 상영 시간 (분 단위) |
| **start_date** | DATE | NOT NULL | 상영 시작 가능일 |
| **end_date** | DATE | NOT NULL | 상영 종료 예정일 |

#### 2. `screening_rooms` (상영관 정보)
| 컬럼명 | 타입 | 제약 조건 | 설명 |
| :--- | :--- | :--- | :--- |
| **id** | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 상영관 고유 식별자 |
| **name** | VARCHAR(255) | NOT NULL | 상영관 이름 (예: 1관) |
| **operating_start_time** | TIME | NOT NULL | 영업 시작 시간 |
| **operating_end_time** | TIME | NOT NULL | 영업 종료 시간 |

#### 3. `screenings` (상영 스케줄)
| 컬럼명 | 타입 | 제약 조건 | 설명 |
| :--- | :--- | :--- | :--- |
| **id** | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 상영 회차 고유 식별자 |
| **movie_id** | BIGINT | FOREIGN KEY (movies.id) | 상영될 영화 ID |
| **room_id** | BIGINT | FOREIGN KEY (screening_rooms.id) | 상영될 장소 ID |
| **start_time** | TIMESTAMP | NOT NULL | 상영 시작 일시 |

#### 4. `reservations` (예매 정보)
| 컬럼명 | 타입 | 제약 조건 | 설명 |
| :--- | :--- | :--- | :--- |
| **id** | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 예매 고유 번호 |
| **used_points** | INT | NOT NULL | 사용된 포인트 |
| **payment_method** | VARCHAR(50) | NOT NULL | 결제 수단 (카드, 현금 등) |
| **total_price** | INT | NOT NULL | 총 결제 금액 |
| **created_at** | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 예매 생성 일시 |

#### 5. `reserved_seats` (예약된 좌석 상세)
| 컬럼명 | 타입 | 제약 조건 | 설명 |
| :--- | :--- | :--- | :--- |
| **id** | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 좌석 예약 고유 식별자 |
| **reservation_id** | BIGINT | FOREIGN KEY (reservations.id) | 연결된 예매 ID |
| **screening_id** | BIGINT | FOREIGN KEY (screenings.id) | 상영 회차 ID |
| **seat_row** | VARCHAR(5) | NOT NULL | 좌석 행 (예: A, B) |
| **seat_column** | INT | NOT NULL | 좌석 열 (예: 1, 2) |


## 1 | 2단계 기능 목록

## 도메인 - 모델

### 1. 영화

- [x] 영화 제목을 가진다
- [x] 영화 상영 길이를 가진다
- [x] 영화 상영 기간을 가진다

### 1-1. 영화 제목

- [x] 제목을 가진다

### 1-2. 상영 길이

- [x] 분 단위의 상영길이를 가진다

### 1-3. 상영 기간

- [x] 시작일과 종료일을 가진다

### 2. 좌석

- [x] 행과 열을 가진다
    - 행은 알파벳(A~J)으로 이루어져 있다
    - 열은 숫자로 이루어져 있다(1~12)
- [x] 예약 상태를 가진다
- [x] 좌석의 등급을 안다

### 3. 등급

- [x] 등급에 대한 금액을 가진다
    - S석: 18_000
    - A석: 15_000
    - B석: 12_000

### 4. 상영관

- [x] 상영관 이름을 가진다
- [x] 상영관 운영 시간 가진다
- [x] 상영관에 속한 예약 가능한 좌석 목록을 가진다.

### 5. 시간 범위

- [x] 시작, 종료 시간을 가진다
- [x] 시간이 겹치는 지 판단한다

### 6. 상영 정보

- [x] 영화를 가진다
- [x] 상영 날짜 및 시작 시간을 가진다 (`LocalDateTime`)
- [x] 상영관을 가진다
- [x] 상영 시작 및 종료 시간을 가진다
- [x] 해당 상영 회차의 예약 가능 좌석 목록을 관리한다
    - 상영관에 속한 좌석을 기반으로 예약 상태와 함께 초기화한다

### 7. 상영 정보 목록

- [x] 상영 정보를 가진다
- [x] 영화별 상영 목록을 알고있다

### 8. 티켓

- [x] 영화 상영 정보를 가진다
- [x] 예매한 좌석들을 가진다

### 9. 장바구니

- [x] 티켓들을 가진다
- [x] 티켓을 추가한다

### 10. 할인

- [x] 카드 결제 시 할인을 적용한다
- [x] 현금 결제 시 할인을 적용한다
- [x] 무비데이 조건에 해당하는 경우 할인을 적용한다
- [x] 특정 시간대 조건에 해당하는 경우 할인을 적용한다

### 10. 결제 시스템

- [x] 할인 정책을 알고있다.
- [x] 결제 정보를 받아서 총 결제 금액을 계산한다
    - 장바구니
    - 결제 수단(카드, 현금)
    - 포인트

## View

### 예매 시작

- [x] 예매 시작 안내 메세지를 출력한다
- [x] 새 예매 생성여부를 입력받는다(Y/N)

### 영화 선택

- [x] 예매할 영화 제목을 입력받는다

### 예매 날짜 입력

- [x] 예매 날짜를 입력받는다 (yyyy-MM-dd)

### 예매할 영화 선택

- [x] 예매할 영화의 상영 번호을 입력받는다

### 예매 좌석 선택

- [x] 좌석 배치도를 출력한다
- [x] 예약할 좌석을 입력받는다 (${Row}${Column})

### 장바구니 출력

- [x] 장바구니에 추가된 예매 정보를 출력한다

### 추가 예매 여부 입력

- [x] 추가 예매 여부를 입력받는다(Y/N)

### 결제

- [x] 사용할 포인트를 입력받는다
- [x] 결제 수단을 입력받는다
    - `1`: 신용카드
    - `2`: 현금
- [x] 총 결제 금액을 출력한다
- [x] 예매 확정 여부를 입력받는다
- [x] 예매 결과를 출력한다

---

## 테스트 목록

### DB 테스트

#### JdbcConnection
- [x] H2 데이터베이스 연결을 가져올 수 있다
- [x] 연결된 커넥션은 유효해야 한다
- [x] 설정된 DB URL이 올바른지 확인한다

#### MovieRepository
- [x] findAll은 저장된 모든 영화 목록을 반환한다
- [x] findById는 존재하는 ID로 조회 시 해당 영화를 반환한다
- [x] findById는 존재하지 않는 ID로 조회 시 예외를 던진다

#### ScreeningRoomRepository
- [x] findById는 존재하는 상영관 ID로 조회 시 상영관 객체를 반환한다
- [x] findById는 존재하지 않는 ID로 조회 시 예외를 던진다

#### ScreeningRepository
- [x] findAll은 모든 상영 정보와 예약된 좌석 상태를 반환한다

#### ReservationRepository
- [x] save는 예약 정보와 좌석 정보를 저장한다

### Spring 테스트
- [x] 영화 목록 조회 요청 시 200 OK를 반환하고 영화와 상영 정보가 포함된다
- [x] 정상적인 예매 요청 시 210 Created 응답과 함께 예매 내역을 반환한다
- [x] 여러 영화를 한 번에 예매할 때 각 상영별 할인이 올바르게 적용되어 최종 금액이 반환된다

### 영화 테스트

- [x] 영화 제목이 공백이 아닌 경우 Title이 생성된다
- [x] 영화 제목이 공백일 경우 예외를 던진다
- [x] 영화의 상영 길이가 0초과일 경우 RunningTime이 생성된다
- [x] 영화의 상영 길이가 0이하일 경우 예외를 던진다
- [x] 시작일이 종료일보다 앞설 경우 ScreeningPeriod가 생성된다
- [x] 종료일이 시작일보다 앞 설 경우 예외를 던진다
- [x] 영화는 제목, 상영 길이, 상영 기간을 가진다

### 좌석

- [x] 행과 열을 가진다
- [x] 행이 A~J 범위가 아닐 경우 예외를 던진다
- [x] 행이 알파벳이 아닌 문자가 포함될 경우 예외를 던진다
- [x] 열이 1~12 범위가 아닐 경우 예외를 던진다
- [x] 좌석 위치에 따른 등급을 안다
- [x] 좌석의 초기 예약 상태는 예약 가능한 상태이다
- [x] 좌석은 자신이 예약가능한지를 판단할 수 있다

### 좌석 등급

- [x] 등급에 대한 가격을 가진다
- [x] 행과 열에 대한 등급을 반환한다

### 상영관

- [x] 이름, 운영시간, 좌석 목록을 가진다
- [x] 이름이 공백일 경우 예외를 던진다
- [x] 좌석이 없을 경우 예외를 던진다

### 시간 범위

- [x] 종료시간이 시작시간보다 앞서면 예외를 던진다
- [x] 시간 범위가 겹치지 않으면 false를 반환한다
- [x] 시간 범위가 겹치면 true를 반환한다

### 상영 정보

- [x] 상영은 영화, 상영관, 상영 날짜 및 시간을 가진다
- [x] 상영 시작 시간과 영화 상영 길이에 따른 시작 및 종료 시간을 가진다
- [x] 상영 시간이 상영관의 운영시간에 포함되지 않을 경우 예외를 던진다
- [x] 좌석 예약 시 해당 좌석이 예약된 새로운 Screening 객체를 반환한다
- [x] 상영 시간이 겹치는지 확인할 수 있다
- [x] 동일한 ID를 가진 상영인지 확인할 수 있다

### 티켓

- [x] 티켓은 상영 정보와 예매 좌석 정보를 가진다
- [x] 두 티켓의 상영 시간이 겹치는 티켓이라면 true를 반환한다
- [x] 두 티켓이 동일한 상영 회차의 티켓이라면 true를 반환한다
- [x] 두 티켓 간에 동일한 좌석이 포함되어 있다면 true를 반환한다
- [x] SeatPositions 생성 시 위치에 중복이 있을 경우 예외를 던진다
- [x] SeatPositions 생성 시 위치에 중복이 없다면 정상적으로 생성된다

### 장바구니

- [x] TicketBucket은 Tickets를 가진다
- [x] 티켓을 추가할 때, 상영시간이 겹치면 예외를 던진다
- [x] 상영 시간이 겹치지 않는 티켓을 추가하면 티켓이 추가된 장바구니를 반환한다

### 상영 정보 목록

- [x] 상영 정보 리스트를 가진다
- [x] 한 영화가 동시에 상영될 경우 예외를 던진다

### 할인

- [x] 의도한 티켓 할인(무비데이 1000원, 시간할인 2000원)이 적용된다
- [x] 의도한 결제 할인(카드 5%)이 적용된다
- [x] 카드 결제 할인 시 5퍼센트의 할인 금액이 반환된다
- [x] 현금 결제 할인 시 2퍼센트의 할인 금액이 반환된다
- [x] 날짜가 무비데이 조건(10, 20, 30일)에 해당하는 경우 할인 금액(1000원)을 반환한다
- [x] 특정 시간대 조건(11시00분 이전, 20시00분 이후)에 해당하는 경우 할인 금액(2000원)을 반환한다

### 결제 시스템

- [x] 무비데이, 시간 할인, 포인트 차감, 결제 수단 할인이 올바른 순서로 적용된다
- [x] 할인 적용 후 금액이 0보다 작을 경우 예외를 던진다
